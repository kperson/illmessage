Resources:
  %{ for sub in subscriptions ~}
  
  ${title(replace("hello world_bob", "/\\W/", ""))}:
    Type: AWS::CloudFormation::CustomResource
    Properties:
      Exchange: ${sub.exchange}
      BindingKey: ${sub.binding_key}
      Queue: ${sub.queue}
      ServiceToken: ${service_token}
  %{ endfor ~}
